server {
    listen 80;
    server_name _;
    root /usr/share/nginx/html;
    index index.html;

    # VitePress cleanUrls: /guide/foo → guide/foo.html
    location / {
        try_files $uri $uri.html $uri/ =404;
    }

    error_page 404 /404.html;

    location = /up {
        default_type text/plain;
        return 200 'ok\n';
    }
}
